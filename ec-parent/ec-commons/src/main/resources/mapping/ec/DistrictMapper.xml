<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.imexue.ec.common.mapper.ec.DistrictMapper" >
  <resultMap id="BaseResultMap" type="cn.imexue.ec.common.model.District" >
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="city_id" property="cityId" jdbcType="BIGINT" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="sort_no" property="sortNo" jdbcType="INTEGER" />
  </resultMap>
  
  <select id="selectByCityId" parameterType="java.lang.Long" resultType="cn.imexue.ec.common.model.vo.IdNameVO">
  		select id, name from t_district  where city_id=#{cityId,jdbcType=BIGINT}
  		order by sort_no
  </select>
  
  <select id="selectByIds" resultType="cn.imexue.ec.common.model.vo.IdNameVO">
  		select id, name from t_district where  id in
  		  <foreach collection="list" item="districtIds" open="(" close=")" separator=",">
	            #{districtIds}
	     </foreach>
	     order by sort_no
  </select>
  
  
  <select id="selectAll" resultType="cn.imexue.ec.common.model.vo.IdNameVO">
  		select id,name from t_district
  </select>
  
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.imexue.ec.common.mapper.ec.CityMapper" >
  <resultMap id="BaseResultMap" type="cn.imexue.ec.common.model.City" >
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="province_id" property="provinceId" jdbcType="BIGINT" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="calling_code" property="callingCode" jdbcType="VARCHAR" />
    <result column="sort_no" property="sortNo" jdbcType="INTEGER" />
  </resultMap>
  
  <select id="selectByProvinceId" parameterType="java.lang.Long" resultType="cn.imexue.ec.common.model.vo.IdNameVO">
  		select id, name from t_city where province_id=#{provinceId,jdbcType=BIGINT}
  		order by sort_no
  </select>
  
  <select id="selectByIds" resultType="cn.imexue.ec.common.model.vo.IdNameVO">
  		select id, name from t_city where  id in
  		  <foreach collection="list" item="cityIds" open="(" close=")" separator=",">
	            #{cityIds}
	     </foreach>
	     order by sort_no
  </select>
  
    <select id="selectAll" resultType="cn.imexue.ec.common.model.vo.IdNameVO">
  		select id,name from t_city
  </select>
  
  
</mapper>